#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long x,y,s;
    while(scanf("%lld %lld",&x,&y)!=EOF){
            s=abs(x-y);
        cout<<s<<endl;
    }
    return 0;
}
